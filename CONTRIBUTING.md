# 协作约定

## 放置规则

- 个人学习文档放在自己的 `documents/`。
- AI 对话记录放在自己的 `ai-conversations/`。
- 一次提交尽量只处理一个主题，避免把三个人无关的修改混在一起。
- 不提交密码、令牌、个人隐私、超大安装包、构建产物或克隆的第三方仓库。
- 引用外部项目时，优先记录项目链接、版本或 commit，不把整个仓库复制进来。

## 文件命名

建议使用：

```text
YYYY-MM-DD_topic.md
```

AI 对话记录至少写清：

1. 对话日期和使用的 AI；
2. 原始问题或目标；
3. 关键上下文和提示词；
4. 有价值的回答或结论；
5. 自己的验证结果与未解决问题。

## 推荐提交流程

```bash
git pull --rebase
git add <自己的目录>
git commit -m "docs: 简要说明本次学习内容"
git push
```
